package network.data
/*
class StockDatabaseDataSource(private val database: StockTrackerDatabase) :  {

    fun storeIndex(stockIndex: StockIndex) {
        database.stockIndicesQueries.insertOrUpdateIndex(
            stockIndex.symbol,
            stockIndex.currentPrice,
            stockIndex.highPrice,
            stockIndex.lowPrice,
            stockIndex.openPrice,
            stockIndex.previousClosePrice,
            stockIndex.percentChange,
            stockIndex.change,
            stockIndex.timestamp
        )
    }

    fun getAllIndices(): List<StockIndex> {
        return database.stockIndicesQueries.selectAllIndices().executeAsList()
    }

    // Implémentez d'autres méthodes si nécessaire
}
*/